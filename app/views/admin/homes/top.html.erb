<div class = "container">
  <div class = "row">

<h1 class="bg-secondary">管理者用TOPページ</h1>
<h4>TOPページだけど注文履歴一覧画面だよ</h4>

<div class="container">
  <div class="row">
    <div class="col">
      <table class="table">
        <thead>
          <tr>
            <th><%= "購入日時" %></th>
            <th><%= "購入者" %></th>
            <th><%= "注文個数" %></th>
            <th><%= "注文ステータス" %></th>
          </tr>
        </thead>
        <tbody>
          <% @oders.each do |oder| %>
            <% @sum = 0 %>
            <tr>
              <td><%= link_to oder.created_at, admin_oder_path(oder) %></td>
              <td><%= oder.customer.last_name+""+oder.customer.first_name %></td>
              <td><% oder.oder_items.each do |item| %>
                  <% @sum = (@sum+item.amount) %>
                  <% end %>
                  <%= @sum %></td>
              <td><%= oder.status %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>